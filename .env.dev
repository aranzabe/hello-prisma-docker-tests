DATABASE_URL=postgresql://postgres:Chubaca2025@db:5432/ejemploPrisma?schema=public
NODE_ENV=development
PORT=3000

# | Acción                                | Comando Docker                                    |
# | ------------------------------------- | ------------------------------------------------- |
# | Levantar DB + app                     | `docker compose up -d`                            |
# | Detener todo                          | `docker compose down`                             |
# | Entrar en contenedor de app           | `docker compose exec app sh`                      |
# | Ejecutar migraciones desde contenedor | `docker compose exec app yarn prisma migrate dev` |
# | Ejecutar seeders desde contenedor     | `docker compose exec app yarn seed`               |
# | Ver logs de la app                    | `docker compose logs -f app`                      |
# | Acceder a DB desde host               | `psql -h localhost -U postgres -d ejemploPrisma`  |
